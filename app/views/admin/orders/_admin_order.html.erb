<tr>
	<td><%= admin_order.user.email %></td>
	<td><%= admin_order.product.name %></td>
	<td><%= admin_order.quantity %></td>
	<td><%= admin_order.created_at.getlocal.strftime("%e-%m-%y %H:%M") %></td>
</tr>