<h1 class="jumbotron">New Category</h1>
<div class="container">
	<%= simple_form_for [:admin, @category], class: "form-horizonal" do |f| %>
  
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <%= f.submit %>
<% end %>

<%= link_to 'Back to Category List', admin_categories_path %>